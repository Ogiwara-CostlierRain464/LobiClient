<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.ogiwara.test.lobitest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:name=".ThisApplication"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".twitter_login.TwitterLoginActivity" />
        <activity android:name=".mail_login.MailLoginActivity" />
        <activity android:name=".base.MainActivity" />
        <activity android:name=".group.GroupActivity" />
        <activity android:name=".user.UserActivity" />

    </application>

    <!-- TODO Splash -->

</manifest>